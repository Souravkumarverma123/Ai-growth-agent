/**
 * The deterministic economics layer built on top of the frozen contracts.
 * Pure functions only — see CONTRACTS.md §2 (no I/O in `packages/policy`).
 */
export * from "./contribution";
export * from "./campaign-budget";
